# 📋 Что осталось сделать

## ✅ Основной рефакторинг завершен!

Все критические задачи выполнены:

- ✅ Реорганизация по FSD
- ✅ Вынос констант и ENUM
- ✅ Создание API слоя
- ✅ Замена синхронных вызовов на асинхронные
- ✅ Вынос общих UI компонентов
- ✅ Удаление старых файлов

## 🔧 Технические проблемы (не критично)

### 1. Ошибки линтера в DateTimeSelection.tsx

**Статус:** Проблема кеша TypeScript, не реальные ошибки

**Описание:** Линтер показывает ошибки импортов, но пути правильные. Это проблема кеша TypeScript сервера.

**Решение:**

- Перезапустить TypeScript сервер в IDE
- Или игнорировать (пути корректны, код работает)

## 📝 TODO комментарии в коде (будущие улучшения)

### 1. API запросы для слотов времени

**Файлы:**

- `src/features/date-time-selection/ui/DateTimeSelection.tsx:94`
- `src/features/specialist-selection/ui/SpecialistSelection.tsx:94, 100`
- `src/features/department-selection/ui/DepartmentSpecialistsSelection.tsx:61, 67`

**Задача:** Заменить моковые данные слотов времени на реальные API запросы

### 2. Сохранение данных на сервер

**Файлы:**

- `src/features/appointment-details/ui/AppointmentDetails.tsx:186, 225`

**Задача:**

- Отправка данных записи на сервер
- Сохранение нового питомца через API

### 3. Функциональность подтверждения записи

**Файлы:**

- `src/features/appointment-confirmation/ui/AppointmentConfirmation.tsx:62, 68, 98`

**Задача:**

- Реализовать добавление в календарь
- Реализовать построение маршрута
- Открыть модальное окно с информацией о враче

### 4. Обработка выбора слотов времени

**Файлы:**

- `src/features/specialist-selection/ui/SpecialistSelection.tsx:201`
- `src/features/department-selection/ui/DepartmentSpecialistsSelection.tsx:141`

**Задача:** Реализовать логику выбора слотов времени

## 🚀 Опциональные улучшения

### 1. Создать хуки для работы с API (`shared/hooks/`)

**Преимущества:**

- Переиспользование логики загрузки данных
- Единообразная обработка состояний (loading, error, data)
- Упрощение компонентов

**Хуки для создания:**

```typescript
// shared/hooks/useBranches.ts
export function useBranches() {
  // Загрузка филиалов с обработкой состояний
}

// shared/hooks/useEmployees.ts
export function useEmployees(branchId?: number, departmentId?: number) {
  // Загрузка сотрудников
}

// shared/hooks/usePets.ts
export function usePets(phone: string) {
  // Загрузка питомцев
}

// shared/hooks/useTimeSlots.ts
export function useTimeSlots(employeeId: number, date: Date) {
  // Загрузка доступных слотов времени
}
```

### 2. Улучшить обработку ошибок в API слое

**Текущее состояние:** Базовая обработка с `console.error`

**Улучшения:**

- Централизованная обработка ошибок
- Показ пользователю понятных сообщений об ошибках
- Retry механизм для сетевых ошибок
- Разные стратегии обработки для разных типов ошибок

**Пример:**

```typescript
// shared/api/error-handler.ts
export class ApiError extends Error {
  constructor(message: string, public statusCode?: number, public code?: string) {
    super(message);
  }
}

export function handleApiError(error: unknown): string {
  // Логика обработки и форматирования ошибок
}
```

### 3. Добавить конфигурацию API

**Файл:** `shared/config/api.ts`

**Содержимое:**

```typescript
export const API_CONFIG = {
  baseURL: process.env.API_BASE_URL || 'https://api.example.com',
  timeout: 30000,
  retryAttempts: 3,
  // и т.д.
};
```

### 4. Использовать React Query или SWR

**Преимущества:**

- Автоматическое кеширование
- Автоматическая повторная валидация
- Оптимистичные обновления
- Упрощение управления состоянием

**Пример:**

```typescript
// С React Query
const { data, isLoading, error } = useQuery(['branches'], () => branchesApi.getAll());
```

### 5. Добавить тесты

**Типы тестов:**

- Unit тесты для утилит (`shared/lib/`)
- Unit тесты для компонентов (`shared/ui/`)
- Интеграционные тесты для фич
- E2E тесты для критических сценариев

**Инструменты:**

- Jest + React Testing Library
- Vitest (альтернатива Jest)
- Playwright (E2E)

### 6. Настроить ESLint правила для FSD

**Правила:**

- Запрет импортов из верхних слоев в нижние
- Запрет импортов между фичами
- Требование использования `shared` для общих ресурсов

**Пример конфигурации:**

```json
{
  "rules": {
    "import/no-restricted-paths": [
      "error",
      {
        "zones": [
          {
            "target": "./src/features/**",
            "from": "./src/features/**",
            "except": ["./src/features/**/index.ts"]
          }
        ]
      }
    ]
  }
}
```

### 7. Создать Storybook для UI компонентов

**Преимущества:**

- Документация компонентов
- Визуальное тестирование
- Изоляция разработки компонентов

**Компоненты для документирования:**

- Avatar (все размеры)
- InfoCard (с иконкой, с аватаром)
- InfoListItem (с действием, без действия)
- EmptyState
- BackButton

### 8. Оптимизация производительности

**Задачи:**

- React.memo для компонентов, которые редко меняются
- useMemo/useCallback где необходимо
- Code splitting для больших компонентов
- Lazy loading для виджетов

### 9. Улучшить типизацию

**Задачи:**

- Строгие типы для всех пропсов
- Типы для всех API ответов
- Типы для состояний виджета
- Убрать `any` типы (если есть)

### 10. Добавить CI/CD

**Задачи:**

- Автоматический запуск тестов
- Проверка типов TypeScript
- Линтинг кода
- Сборка проекта
- Деплой (если нужно)

## 📊 Приоритеты

### Высокий приоритет (рекомендуется)

1. ✅ **Создать хуки для API** - упростит код компонентов
2. ✅ **Улучшить обработку ошибок** - улучшит UX
3. ✅ **Добавить конфигурацию API** - подготовка к интеграции

### Средний приоритет (желательно)

4. React Query/SWR - улучшит управление состоянием
5. Тесты - повысит надежность
6. ESLint правила для FSD - предотвратит ошибки архитектуры

### Низкий приоритет (опционально)

7. Storybook - улучшит документацию
8. Оптимизация производительности - если есть проблемы
9. CI/CD - для автоматизации

## 🎯 Рекомендации

**Сейчас проект готов к:**

- ✅ Разработке новых фич
- ✅ Интеграции с реальным API (просто заменить моки)
- ✅ Дальнейшему масштабированию

**Следующие шаги (по желанию):**

1. Если планируется интеграция с бэкендом - создать хуки и улучшить обработку ошибок
2. Если команда растет - добавить тесты и ESLint правила
3. Если нужна документация - настроить Storybook

---

**Вывод:** Основной рефакторинг завершен! Остались только опциональные улучшения и будущие задачи (TODO).
